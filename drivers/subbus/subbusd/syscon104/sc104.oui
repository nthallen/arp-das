<package> sc104
<include> subbusd
<follow> msg
<include> "sc104.h"

<opts> "i:"

<sort>
	-i <IRQ> (default is IRQ 9)

<init>
  sc104_init_options(argc, argv);

<package> msghdr_default
<vars>
  const char *msg_hdr_default = "SB/SC104";
