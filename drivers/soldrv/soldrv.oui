<package> soldrv
<include> msg
<include> "sol.h"
<follow> msg
<opts> "d:"

<init>
  soldrv_init_options(argc, argv);

<synopsis>
%C [options] filename

<sort>
	-d <hex address> cache address breakpoint (default 0x1000)

<unsort>
<blank>
Addresses greater than or equal to the breakpoint are handled
using cache_write().

<package> msghdr_default
<var>
  char *msg_hdr_default = "sol";
