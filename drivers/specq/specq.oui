<package> specq
<include> msg
<follow> msg
<include> "specq.h"

<opts> "H:"
<sort>
	-H host  Host address
<init>
  specq_opt_init(argc,argv);

<package> msghdr_default
<var>
  char *msg_hdr_default = "specq";
